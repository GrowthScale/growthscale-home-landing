# Este arquivo foi desabilitado para evitar conflitos
# Use ci-cd.yml como workflow principal
name: GrowthScale Tests (DISABLED)

on:
  # Desabilitado para evitar conflitos
  # push:
  #   branches: [ main, develop ]
  # pull_request:
  #   branches: [ main, develop ]

env:
  NODE_VERSION: '20'

jobs:
  disabled:
    name: Disabled - Use ci-cd.yml
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "Use ci-cd.yml as the main workflow"
